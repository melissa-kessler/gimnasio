"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.processFullPath = void 0;
const path_1 = __importDefault(require("path"));
const regexPathSeparator = path_1.default.sep === "/" ? "/" : "\\\\";
const slash = (s) => {
    return s.replace(/\\/g, "/");
};
const processFullPath = (fullPath) => {
    const cwd = process.cwd();
    const noCwdPath = fullPath.replace(cwd, "");
    const pathSeparationPattern = new RegExp(`(${regexPathSeparator}.+${regexPathSeparator})(.+)$`);
    const pathSeparationResult = noCwdPath.match(pathSeparationPattern);
    const path = pathSeparationResult === null || pathSeparationResult === void 0 ? void 0 : pathSeparationResult[1];
    const file = pathSeparationResult === null || pathSeparationResult === void 0 ? void 0 : pathSeparationResult[2];
    return {
        path: path && slash(path),
        file: file && slash(file),
    };
};
exports.processFullPath = processFullPath;
